# RectifyCLIP — pinned dependencies (CPU-only, Python 3.13)
# Install with: .venv\Scripts\python.exe -m pip install -r requirements.txt
# CPU-only PyTorch wheels are served from a separate index; pip reads this
# extra-index-url line automatically when installing this file.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.6.0
torchvision==0.21.0
open_clip_torch==2.29.0
opencv-python==4.10.0.84
numpy==2.1.3
pandas==2.2.3
matplotlib==3.9.2
scikit-learn==1.5.2
scipy==1.18.1
pyyaml==6.0.2
Pillow==10.4.0
